{
  "name": "quarto-legends",
  "version": "0.0.1",
  "private": true,
  "description": "Experience the game of Quarto like never before.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "pulp --watch test --main Main.Test",
    "watch": "spago build --watch",
    "build": "spago build",
    "server": "python -m SimpleHTTPServer",
    "bundle": "webpack --config webpack.spago.js",
    "scss": "scss --watch css/main.scss:css/main.css",
    "webpack": "./node_modules/.bin/webpack --watch --config webpack.config.js"
  },
  "author": "Dominck Gendill",
  "license": "LicenseRef-LICENSE",
  "devDependencies": {
    "webpack": "^2.2.1"
  },
  "dependencies": {
    "window": "^3.1.2"
  }
}
